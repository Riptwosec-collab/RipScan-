{
  "name": "ripscan-vercel",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check web/app.js && node --check web/upgrade.js && node --check web/advanced.js && node --check web/sw.js",
    "build": "npm run check && node build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
