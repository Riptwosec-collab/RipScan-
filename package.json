{
  "name": "ripscan-vercel",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check web/app.js && node --check web/upgrade.js",
    "build": "node build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
